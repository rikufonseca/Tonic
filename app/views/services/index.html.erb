  <%
  set_meta_tags(
    title: "Price Mani-pedi and more, nail polish, Nail Salon Tonic in Athens",
    description: "Pricelist of the services proposed by Beauty Salon Tonic with the best nail polish brandn in Athens",
    keywords: "Manicure, Pedicure, Nail polish, OPI, ESSIE, Victoria veen, Gel, Acrygel, semi-permanent, eyebrows"
  )
  %>
  <% if params[:locale] == "en" || params[:locale].nil? %>
    <main class="all-frame margin-frame" data-navbar-target="pageup">
      <div class="inside-box">
        <div class="border-tonic-color">
          <h3 class="underline-title"> <%= t('.mano') %> </h3>
        </div>
        <div class="border-tonic-color">
          <h4 class="underline-title sub-title"> <%= t('.polish') %> </h4>
        </div>
        <section class="grid-container">
          <div class="grid-item">
            <% last_name = nil %>
            <% grouped_services = @services[:services_manicure_en].group_by { |service| service.name_en } %>

            <% grouped_services.each do |name_en, services| %>
              <% if last_name != name_en %>
                <% last_name = name_en %>
                <h5 class="marge" <%= 'style="margin-top: 36px;"' if last_name == grouped_services.keys.first %>>
                  <%= name_en %>
                </h5>
              <% end %>
              <% services.each do |service| %>
                <div class="inline">
                  <h6 class="text"> <%= service.sub_name_en %> </h6>
                  <p> € <%= service.price %> </p>
                </div>
                <p class="left-aligned"> <%= service.description_en %> </p>
              <% end %>
            <% end %>
          </div>
        </section>
        <div class="border-tonic-color upper-tonic-color">
          <h3 class="underline-title"> <%= t('.pedi') %></h3>
        </div>
        <div class="border-tonic-color">
          <h4 class="underline-title sub-title"> <%= t('.polish') %> </h4>
        </div>
        <section class="grid-container">
          <div class="grid-item">
            <% last_name = nil %>
            <% grouped_services = @services[:services_pedicure_en].group_by { |service| service.name_en } %>
            <% grouped_services.each do |name_en, services| %>
              <% if last_name != name_en %>
                <% last_name = name_en %>
                <h5 class="marge" <%= 'style="margin-top: 36px;"' if last_name == grouped_services.keys.first %>>
                  <%= name_en %>
                </h5>
              <% end %>
              <% services.each do |service| %>
                <div class="inline">
                  <h6 class="text"> <%= service.sub_name_en %> </h6>
                  <p> € <%= service.price %> </p>
                </div>
                <p class="left-aligned"> <%= service.description_en %> </p>
              <% end %>
            <% end %>
          </div>
        </section>
        <div class="border-tonic-color upper-tonic-color">
          <h3 class="underline-title"> <%= t('.men') %></h3>
        </div>
        <section class="grid-container pad_all">
          <div class="grid-item padding-out-tel">
            <% last_name = nil %>
            <% grouped_services = @services[:services_men_en].group_by { |service| service.name_en } %>
            <% grouped_services.each do |name_en, services| %>
              <% if last_name != name_en %>
                <% last_name = name_en %>
                <h5 class="marge" <%= 'style="margin-top: 36px;"' if last_name == grouped_services.keys.first %>>
                  <%= name_en %>
                </h5>
              <% end %>
              <% services.each do |service| %>
                <div class="inline">
                  <h6 class="text"> <%= service.sub_name_en %> </h6>
                  <p> € <%= service.price %> </p>
                </div>
                <p class="left-aligned"> <%= service.description_en %> </p>
              <% end %>
            <% end %>
          </div>
        </section>
        <div class="border-tonic-color upper-tonic-color">
          <h3 class="underline-title"> NAILS ART </h3>
        </div>
        <section class="grid-container pad_all">
          <div class="grid-item padding-out-tel">
            <% last_name = nil %>
            <% grouped_services = @services[:services_art_en].group_by { |service| service.name_en } %>
            <% grouped_services.each do |name_en, services| %>
              <% if last_name != name_en %>
                <% last_name = name_en %>
                <h5 class="marge" <%= 'style="margin-top: 36px;"' if last_name == grouped_services.keys.first %>>
                  <%= name_en %>
                </h5>
              <% end %>
              <% services.each do |service| %>
                <div class="inline">
                  <h6 class="text"> <%= service.sub_name_en %> </h6>
                  <p> <%= service.description_en %> € <%= service.price %></p>
                </div>
              <% end %>
            <% end %>
          </div>
        </section>
        <div class="border-tonic-color upper-tonic-color">
          <h3 class="underline-title"> EXTRA </h3>
        </div>
        <section class="grid-container pad_all">
          <div class="grid-item padding-out-tel">
            <% last_name = nil %>
            <% grouped_services = @services[:services_extra_en].group_by { |service| service.name_en } %>
            <% grouped_services.each do |name_en, services| %>
              <% if last_name != name_en %>
                <% last_name = name_en %>
                <h5 class="marge" <%= 'style="margin-top: 36px;"' if last_name == grouped_services.keys.first %>>
                  <%= name_en %>
                </h5>
              <% end %>
              <% services.each do |service| %>
                <div class="inline">
                  <h6 class="text"> <%= service.sub_name_en %> </h6>
                  <p> <%= service.description_en %> € <%= service.price %></p>
                </div>
              <% end %>
            <% end %>
          </div>
        </section>
        </div>
      </div>
    </main>
  <% else %>
    <main class="all-frame margin-frame" data-navbar-target="pageup">
      <div class="inside-box">
        <div class="border-tonic-color">
          <h3 class="underline-title"> <%= t('.mano') %> </h3>
        </div>
        <div class="border-tonic-color">
          <h4 class="underline-title sub-title"> <%= t('.polish') %> </h4>
        </div>
        <section class="grid-container">
          <div class="grid-item">
            <% last_name = nil %>
            <% grouped_services = @services[:services_manicure_gr].group_by { |service| service.name_gr } %>

            <% grouped_services.each do |name_gr, services| %>
              <% if last_name != name_gr %>
                <% last_name = name_gr %>
                <h5 class="marge" <%= 'style="margin-top: 36px;"' if last_name == grouped_services.keys.first %>>
                  <%= name_gr %>
                </h5>
              <% end %>
              <% services.each do |service| %>
                <div class="inline">
                  <h6 class="text"> <%= service.sub_name_gr %> </h6>
                  <p> € <%= service.price %> </p>
                </div>
                <p class="left-aligned"> <%= service.description_gr %> </p>
              <% end %>
            <% end %>
          </div>
        </section>
        <div class="border-tonic-color upper-line-pink">
          <h3 class="underline-title"> <%= t('.pedi') %></h3>
        </div>
        <div class="border-tonic-color">
          <h4 class="underline-title sub-title"> <%= t('.polish') %> </h4>
        </div>
        <section class="grid-container">
          <div class="grid-item">
            <% last_name = nil %>
            <% grouped_services = @services[:services_pedicure_gr].group_by { |service| service.name_gr } %>
            <% grouped_services.each do |name_gr, services| %>
              <% if last_name != name_gr %>
                <% last_name = name_gr %>
                <h5 class="marge" <%= 'style="margin-top: 36px;"' if last_name == grouped_services.keys.first %>>
                  <%= name_gr %>
                </h5>
              <% end %>
              <% services.each do |service| %>
                <div class="inline">
                  <h6 class="text"> <%= service.sub_name_gr %> </h6>
                  <p> € <%= service.price %> </p>
                </div>
                <p class="left-aligned"> <%= service.description_gr %> </p>
              <% end %>
            <% end %>
          </div>
        </section>
        <div class="border-tonic-color upper-line-pink">
          <h3 class="underline-title"> <%= t('.men') %></h3>
        </div>
        <section class="grid-container pad_all">
          <div class="grid-item padding-out-tel">
            <% last_name = nil %>
            <% grouped_services = @services[:services_men_gr].group_by { |service| service.name_gr } %>
            <% grouped_services.each do |name_gr, services| %>
              <% if last_name != name_gr %>
                <% last_name = name_gr %>
                <h5 class="marge" <%= 'style="margin-top: 36px;"' if last_name == grouped_services.keys.first %>>
                  <%= name_gr %>
                </h5>
              <% end %>
              <% services.each do |service| %>
                <div class="inline">
                  <h6 class="text"> <%= service.sub_name_gr %> </h6>
                  <p> € <%= service.price %> </p>
                </div>
                <p class="left-aligned"> <%= service.description_gr %> </p>
              <% end %>
            <% end %>
          </div>
        </section>
        <div class="border-tonic-color upper-line-pink">
          <h3 class="underline-title"> NAILS ART </h3>
        </div>
        <section class="grid-container pad_all">
          <div class="grid-item padding-out-tel">
            <% last_name = nil %>
            <% grouped_services = @services[:services_art_gr].group_by { |service| service.name_gr } %>
            <% grouped_services.each do |name_gr, services| %>
              <% if last_name != name_gr %>
                <% last_name = name_gr %>
                <h5 class="marge" <%= 'style="margin-top: 36px;"' if last_name == grouped_services.keys.first %>>
                  <%= name_gr %>
                </h5>
              <% end %>
              <% services.each do |service| %>
                <div class="inline">
                  <h6 class="text"> <%= service.sub_name_gr %> </h6>
                  <p> <%= service.description_gr %> € <%= service.price %></p>
                </div>
              <% end %>
            <% end %>
          </div>
        </section>
        <div class="border-tonic-color upper-line-pink">
          <h3 class="underline-title"> EXTRA </h3>
        </div>
        <section class="grid-container pad_all">
          <div class="grid-item padding-out-tel">
            <% last_name = nil %>
            <% grouped_services = @services[:services_extra_gr].group_by { |service| service.name_gr } %>
            <% grouped_services.each do |name_gr, services| %>
              <% if last_name != name_gr %>
                <% last_name = name_gr %>
                <h5 class="marge" <%= 'style="margin-top: 36px;"' if last_name == grouped_services.keys.first %>>
                  <%= name_gr %>
                </h5>
              <% end %>
              <% services.each do |service| %>
                <div class="inline">
                  <h6 class="text"> <%= service.sub_name_gr %> </h6>
                  <p> <%= service.description_gr %> € <%= service.price %></p>
                </div>
              <% end %>
            <% end %>
          </div>
        </section>
        </div>
      </div>
    </main>
  <% end %>
